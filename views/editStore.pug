extends layout 

include mixins/_storeForm 

block content 
    .container
        h2 #{title} 
        //-if we pass an object "store" this is an edit, if not, this is new 
        +storeForm(store) 